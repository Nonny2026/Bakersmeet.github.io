import { defineconfig, env } from
  "@prisma/config";

export default defineconfig({schema: "./schema.prisma",
                             datasource: {
                             //This tells prisma to look at 
                             the DATABASE_URL you pasted in Render 
                               url: env("DATABASE_URL"),
                             },
                            });
